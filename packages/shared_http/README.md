# shared_http

Общий HTTP-клиент для межсервисных вызовов.

**Статус: ✅ готово.** Используется в `ai-workflow-service` для вызовов downstream-сервисов.

Импорт: `shared_http`  
Путь: `packages/shared_http/`

`ServiceHttpClient` — retry, таймауты, типизированные ошибки.
